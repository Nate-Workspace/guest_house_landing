/** Stable class names for photo overlays — styles live in globals.css to avoid hydration drift. */

export const heroHomeOverlay = "overlay-home-hero";

export const pageHeroOverlay = "overlay-page-hero";

export const sectionBackgroundOverlay = "overlay-section-bg";

/** @deprecated Use sectionBackgroundOverlay */
export const sectionDarkOverlay = sectionBackgroundOverlay;
